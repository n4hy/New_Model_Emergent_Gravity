% Two real packets. One enclosing ball. Midpoint cancels. Not derived Poisson.
\documentclass[11pt]{article}
\usepackage[margin=1.05in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\newtheorem*{admission}{Admission}

\title{\textbf{Two Packets: One Ball Reads the Pair.\\
The Midpoint Cancels.}}
\author{Robert W.\ McGwier\\
\small CTO, Cohere Technology Group\\
\small GitHub: \texttt{n4hy}}
\date{15 August 2026 --- Version 1}

\begin{document}
\maketitle

\begin{abstract}
\noindent
Papers~48--49 used a point of $M_{\mathrm{hat}}$ or a
60-ball median. This note drops both.

One source-centered $\kappa$ from packet $A$. One
midpoint ball that actually encloses both packets
($R=5$; $R=4$ holds only $82\%$).
$M_{\mathrm{FL}}=\delta S_{AB}/\kappa$ matches
$\sum\delta e_{AB}$ to $0.84\%$.

The real pair density, in the locked DST Poisson
solver: the midpoint acceleration is
$4\times 10^{-5}$ of the characteristic field.
Outside, Poisson matches two-point Coulomb of the
two $\sum\delta e$ masses to $0.8\%$.

Auditor, $N=10$: mass CONFIRMED ($0.38\%$). Midpoint
CONFIRMED. Exterior CONFIRMED.

$a_{AB}=a_A+a_B$ is linear Poisson. I did not call
that a discovery. Not $8\pi G$. Not de~Sitter.
\end{abstract}

\begin{admission}
I dropped the point and the cloud. I kept Poisson.
The pair cancelled in the middle. I did not write
that entanglement derived $1/r^2$.
\end{admission}

\end{document}
